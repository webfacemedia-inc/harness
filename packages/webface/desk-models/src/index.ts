/**
 * @webface/dsh-desk-models — the webfaCe Desk model layer as a profile bundle.
 * The package's substance is `cordis.patch.yml`, declared by the
 * `dsh.bundle.patch` manifest field; this module carries no runtime API.
 * @module @webface/dsh-desk-models
 */

export {}
