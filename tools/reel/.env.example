# Copy to .env (gitignored). The ops key lives on the prod control plane.
DESKAPI_OPS_KEY=
# Control-plane API base. Until the apex cut-over this is the Convex site URL
# directly; after cut-over https://webfacedesk.app works too.
DESK_API_URL=https://webfacedesk.app
